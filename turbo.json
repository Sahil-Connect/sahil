{
  "globalEnv": ["HASURA_GRAPHQL_ADMIN_SECRET"],
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "lint": {},
    "dev": {
      "cache": false,
      "persistent": true
    },
    "codegen": {
      "env": ["HASURA_GRAPHQL_ADMIN_SECRET"]
    }
  }
}
